<!DOCTYPE html>
<html>
  <head>
    <title>FoodClass.</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="UTF-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  </head>

  <body data-controller="transition" data-transition-page-value="<%= controller_name %>_<%= action_name %>">
    <div class="transition transition-1 <%= "is-active" if controller_name == "posts" && action_name == "index" %>"
      data-transition-target="panel">
      <div class="carrot-fall" data-transition-target="carrot">
        <i class="fal fa-carrot"></i>
      </div>
      <div class="title-transition">
        <%= image_tag("logo-fc.jpg") %>
      </div>
    </div>
    <%= render 'shared/navbar' %>
    <%= yield %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
  </body>
</html>
